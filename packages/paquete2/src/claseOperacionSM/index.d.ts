export declare class Operaciones {
    sumar(num1: number, num2: number): number;
    restar(num1: number, num2: number): number;
    multiplicar(num1: number, num2: number): number;
    dividir(num1: number, num2: number): number | string;
}

{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.8.9",
    "del": "^7.1.0",
    "gulp": "^4.0.2",
    "lerna": "^7.4.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "npx tsc -b -v packages",
    "clean": "npx gulp clean:packages",
    "comandos": "lerna run comandos",
    "tsc": "lerna run tsc"
  }
}
